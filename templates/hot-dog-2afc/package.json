{
  "name": "{{kebab name}}",
  "description": "{{description}}",
  "version": "1.0.0",
  "author": "{{contact}}",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx webpack --mode production",
    "build:dev": "npm run build --silent -- --env dbmode=development",
    "build:prod": "npm run build --silent -- --env dbmode=production",
    "start": "npx webpack serve --open --mode development",
    "start:dev": "npm run start --silent -- --env dbmode=development",
    "start:prod": "npm run start --silent -- --env dbmode=production"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/{{githubUsername}}/{{kebab name}}.git"
  },
  "license": "STANFORD ACADEMIC SOFTWARE LICENSE FOR ROAR™",
  "keywords": [
    "roar",
    "literacy",
    "dyslexia",
    "reading",
    "web-assessment"
  ],
  "bugs": {
    "url": "https://github.com/{{githubUsername}}/{{kebab name}}/issues"
  },
  "homepage": "https://github.com/{{githubUsername}}/{{kebab name}}",
  "dependencies": {
  },
  "devDependencies": {
  }
}
