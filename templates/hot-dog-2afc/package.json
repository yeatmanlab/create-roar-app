{
  "name": "{{kebab name}}",
  "description": "{{description}}",
  "version": "1.0.0",
  "author": "{{contact}}",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "npx webpack --mode production",
    "bundle:dev": "npm run build --silent -- --env dbmode=development",
    "bundle:prod": "npm run build --silent -- --env dbmode=production",
    "serve": "npx webpack serve --open --mode development",
    "serve:dev": "npm run start --silent -- --env dbmode=development",
    "serve:prod": "npm run start --silent -- --env dbmode=production"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/{{githubUsername}}/{{kebab name}}.git"
  },
  "license": "STANFORD ACADEMIC SOFTWARE LICENSE FOR ROAR™",
  "keywords": [
    "roar",
    "literacy",
    "dyslexia",
    "reading",
    "web-assessment"
  ],
  "bugs": {
    "url": "https://github.com/{{githubUsername}}/{{kebab name}}/issues"
  },
  "homepage": "https://github.com/{{githubUsername}}/{{kebab name}}",
  "dependencies": {
  },
  "devDependencies": {
  }
}
